<form method="post" action="{{ path('offre_delete', {'idoffre': offre.idoffre}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offre.idoffre) }}">
    <button class="btn">Delete</button>
</form>
